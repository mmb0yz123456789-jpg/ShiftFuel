(function(){
const K="sf_checkout_state";
const $=(s,r=document)=>r.querySelector(s);
const $$=(s,r=document)=>Array.from(r.querySelectorAll(s));
function msg(t,m){try{bookingState.payment.statusType=t;bookingState.payment.status=m;}catch(e){}$$('[data-payment-status]').forEach(x=>{x.dataset.status=t;x.textContent=m;});}
function hide(r=document){$$('.payment-card-box',r).forEach(x=>x.remove());$$('[data-authorize-payment]',r).forEach(b=>{b.textContent='Authorize payment with Stripe';});}
function save(){sessionStorage.setItem(K,JSON.stringify({values:bookingState.values,address:bookingState.address,returning:bookingState.returning}));}
function load(){try{return JSON.parse(sessionStorage.getItem(K)||'null');}catch(e){return null;}}
function apply(s){if(!s)return;if(s.values)Object.assign(bookingState.values,s.values);if(s.address)Object.assign(bookingState.address,s.address);if(s.returning)Object.assign(bookingState.returning,s.returning);}
async function start(panel,button){if(typeof savePanelValues==='function')savePanelValues(panel);const total=calculateTotals();const amount=Math.round((Number(total.estimatedTotal)||0)*100);if(!amount){msg('error','Please complete service details before authorizing payment.');return;}button.disabled=true;button.textContent='Opening Stripe...';msg('warning','Opening Stripe checkout...');try{save();const u=new URL(location.href);['sid','ok','cancel'].forEach(k=>u.searchParams.delete(k));u.hash='booking-flow';const r=await fetch('/api/checkout-authorization',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'create_session',amount_cents:amount,customer_name:customerName(),customer_email:bookingState.values.customerEmail,service_label:serviceLabel(),return_url:u.href})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.url)throw new Error(d.error||'Could not open Stripe.');location.assign(d.url);}catch(e){console.error(e);msg('error',e.message||'Could not open Stripe.');button.disabled=false;button.textContent='Authorize payment with Stripe';}}
async function done(){const p=new URLSearchParams(location.search);if(p.get('cancel')==='1'){apply(load());sessionStorage.removeItem(K);msg('warning','Payment authorization was canceled. No hold was placed.');history.replaceState({},document.title,location.pathname+'#booking-flow');if(typeof renderFlow==='function'&&$('[data-booking-flow]'))renderFlow($('[data-booking-flow]'));return;}const sid=p.get('sid');if(!sid)return;apply(load());msg('warning','Confirming Stripe authorization...');try{const r=await fetch('/api/checkout-authorization',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'verify_session',session_id:sid})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.authorized||!d.payment_intent_id)throw new Error(d.error||'Stripe did not confirm the authorization.');bookingState.payment.authorized=true;bookingState.payment.paymentIntentId=d.payment_intent_id;bookingState.payment.clientSecret='';sessionStorage.removeItem(K);msg('success','Payment authorized with Stripe. You will only be charged after your service is completed.');history.replaceState({},document.title,location.pathname+'#booking-flow');if(typeof renderFlow==='function'&&$('[data-booking-flow]'))renderFlow($('[data-booking-flow]'));}catch(e){console.error(e);msg('error',e.message||'Could not confirm Stripe authorization.');}}
function bind(){hide();document.addEventListener('click',e=>{const b=e.target.closest('[data-authorize-payment]');if(!b)return;const panel=b.closest('.booking-accordion-card');if(!panel)return;e.preventDefault();e.stopImmediatePropagation();start(panel,b);},true);new MutationObserver(()=>hide()).observe(document.documentElement,{childList:true,subtree:true});setTimeout(done,0);}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind);else bind();
})();
