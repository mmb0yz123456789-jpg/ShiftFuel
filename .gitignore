.vercel
.env*

# Node dependencies — never commit these (regenerated by `npm install`).
node_modules/

# Internal planning notes — keep local, not on the public repo.
memory/

# OS / editor cruft
.DS_Store
Thumbs.db
